name: Developer Agent Auto-Implement

on:
  issues:
    types: [assigned]

jobs:
  implement:
    # Note: Triggers on assignment to DEV_AGENT regardless of prior state
    # Status verification at line 86-159 will exit early if not "In Progress"
    # This is intentional - manual assignments will trigger but exit gracefully
    if: >-
      ${{ vars.AGENTS_ENABLED == 'true'
          && vars.DEV_AUTO_IMPLEMENT_ENABLED == 'true'
          && github.event.assignee.login == vars.DEV_AGENT }}
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read

    env:
      GH_TOKEN: ${{ secrets.DEVELOPER_AGENT_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ vars.RELEASE_BRANCH }}
          fetch-depth: 0

      - name: Auth gh
        run: gh auth status

      - name: Write ephemeral config.ini (runner temp)
        shell: bash
        run: |
          set -euo pipefail
          CFG="$RUNNER_TEMP/nyxgpt-config.ini"

          cat > "$CFG" <<EOF
          REPO_OWNER=${{ vars.REPO_OWNER }}
          REPO_NAME=${{ vars.REPO_NAME }}
          PROJECT_OWNER=${{ vars.PROJECT_OWNER }}
          PROJECT_NUMBER=${{ vars.PROJECT_NUMBER }}

          DEV_AGENT=${{ vars.DEV_AGENT }}
          REVIEW_AGENT=${{ vars.REVIEW_AGENT }}
          SCRUM_AGENT=${{ vars.SCRUM_AGENT }}
          HUMAN_OWNER=${{ vars.HUMAN_OWNER }}

          STATUS_FIELD=${{ vars.STATUS_FIELD }}
          STATUS_BACKLOG=${{ vars.STATUS_BACKLOG }}
          STATUS_IN_PROGRESS=${{ vars.STATUS_IN_PROGRESS }}
          STATUS_IN_REVIEW=${{ vars.STATUS_IN_REVIEW }}
          STATUS_FOR_RELEASE=${{ vars.STATUS_FOR_RELEASE }}

          RELEASE_BRANCH=${{ vars.RELEASE_BRANCH }}
          EOF

          echo "NYXGPT_CONFIG_FILE=$CFG" >> "$GITHUB_ENV"
          echo "Wrote ephemeral config at $CFG"

      - name: Verify config loads
        shell: bash
        run: |
          set -euo pipefail
          bash -lc '
            source scripts/agents/lib/gh_project.sh
            load_config
            require_gh_auth
            echo "config ok: $REPO_OWNER/$REPO_NAME project $PROJECT_OWNER#$PROJECT_NUMBER release=$RELEASE_BRANCH"
          '

      - name: Post workflow start comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: '🤖 **Developer Agent**: Auto-implementation workflow triggered.\n\nVerifying issue status...\n\n[View workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})'
            });

      - name: Verify issue is In Progress in ProjectV2 (otherwise exit)
        shell: bash
        env:
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          set -euo pipefail

          echo "::group::Verifying issue #$ISSUE_NUMBER status"
          bash -lc '
            set -euo pipefail
            source scripts/agents/lib/gh_project.sh
            load_config
            require_gh_auth

            issue="$ISSUE_NUMBER"
            project_id="$(get_project_id)"

            q='\''query($project:ID!, $after:String){
              node(id:$project){
                ... on ProjectV2{
                  items(first:100, after:$after){
                    pageInfo{ hasNextPage endCursor }
                    nodes{
                      content{ __typename ... on Issue { number } }
                      fieldValues(first:50){
                        nodes{
                          __typename
                          ... on ProjectV2ItemFieldSingleSelectValue {
                            field{ ... on ProjectV2SingleSelectField { name } }
                            name
                          }
                        }
                      }
                    }
                  }
                }
              }
            }'\''

            after=""
            found=0
            while :; do
              resp="$(graphql "$q" -F project="$project_id" -F after="$after")"

              status="$(echo "$resp" | jq -r --argjson n "$issue" --arg f "$STATUS_FIELD" '\''
                .data.node.items.nodes[]
                | select(.content.__typename=="Issue" and .content.number==$n)
                | (.fieldValues.nodes[]
                    | select(.__typename=="ProjectV2ItemFieldSingleSelectValue" and .field.name==$f)
                    | .name) // empty
              '\'' | head -n 1)"

              if [[ -n "$status" ]]; then
                echo "::notice::Issue #$issue status=$status"
                if [[ "$status" != "$STATUS_IN_PROGRESS" ]]; then
                  echo "::warning::Not In Progress; exiting workflow."
                  exit 0
                fi
                found=1
                break
              fi

              has_next="$(echo "$resp" | jq -r ".data.node.items.pageInfo.hasNextPage")"
              cursor="$(echo "$resp" | jq -r ".data.node.items.pageInfo.endCursor // empty")"
              [[ "$has_next" == "true" && -n "$cursor" ]] || break
              after="$cursor"
            done

            if [[ "$found" != "1" ]]; then
              echo "::warning::Issue not found in project; exiting workflow."
              exit 0
            fi
          '
          echo "::endgroup::"

      - name: Post branch creation comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: '🌿 **Developer Agent**: Creating feature branch...'
            });

      - name: Create branch for issue
        id: branch
        shell: bash
        run: |
          set -euo pipefail
          ISSUE="${{ github.event.issue.number }}"
          echo "::group::Creating branch for issue #$ISSUE"
          BRANCH=$(./scripts/agents/developer_create_branch.sh "$ISSUE" feat auto)
          echo "::notice::Created branch: $BRANCH"
          echo "branch_name=$BRANCH" >> "$GITHUB_OUTPUT"
          echo "::endgroup::"

      - name: Check if PR already exists
        id: check_pr
        shell: bash
        run: |
          set -euo pipefail
          BRANCH="${{ steps.branch.outputs.branch_name }}"

          # Check if PR exists for this branch
          EXISTING_PR=$(gh pr list --head "$BRANCH" --json number -q '.[0].number' || echo "")

          if [[ -n "$EXISTING_PR" ]]; then
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
            echo "existing_pr=$EXISTING_PR" >> "$GITHUB_OUTPUT"
            echo "::notice::PR already exists for branch $BRANCH: #$EXISTING_PR"
          else
            echo "pr_exists=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No existing PR found for branch $BRANCH"
          fi

      - name: Post PR exists notice (if PR exists)
        if: steps.check_pr.outputs.pr_exists == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            const pr_number = '${{ steps.check_pr.outputs.existing_pr }}';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: `✅ **Developer Agent**: PR already exists for this issue.

            PR #${pr_number} is already open and under review. Skipping implementation step.`
            });

      - name: Post implementation start comment
        if: steps.check_pr.outputs.pr_exists == 'false'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            const branch_name = '${{ steps.branch.outputs.branch_name }}';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: `✅ **Developer Agent**: Branch created: \`${branch_name}\`\n\n🤖 Starting Claude Code implementation...\n\nThis may take several minutes. Claude will:\n- Analyze the issue requirements\n- Implement the solution\n- Add/update tests\n- Run the test suite\n- Commit changes\n\nYou can watch progress in the [workflow logs](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}).`
            });

      - name: Run Claude Code to implement issue
        if: steps.check_pr.outputs.pr_exists == 'false'
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          show_full_output: true
          claude_args: '--allowed-tools "Bash,Read,Write,Edit,Glob,Grep"'
          prompt: |
            Implement issue #${{ github.event.issue.number }} on the current branch.

            IMPORTANT: First, check the issue body for "Parent: #N" to determine if this is a sub-issue.
            - If this is a sub-issue (contains "Parent: #N"), reference BOTH parent and sub-issue numbers in commits
            - If this is a parent issue, reference only this issue number

            Requirements:
            - Make the minimal correct change to resolve the issue.
            - Add/update tests for ALL code changes.
            - **MANDATORY VALIDATION BEFORE COMMIT**:
              * If you modified web/ TypeScript files:
                - Run: cd web && npm run type-check
                - ALL type checks MUST PASS before proceeding
                - Fix any TypeScript errors before committing
              * If you modified Python files:
                - Run: mypy src/
                - Fix any type errors before committing
              * If you added backend API endpoints (/api/v1/...):
                - Create corresponding web proxy route in web/src/app/api/v1/.../route.ts
                - Run: ./scripts/validate-web-routes.sh
                - Fix any missing routes before committing
              * Run the test suite with: pytest -v
                - ALL tests MUST PASS (0 failures, 0 errors) before proceeding
                - If ANY test fails or has errors, you MUST fix them before committing
                - DO NOT commit code with failing tests under ANY circumstances
                - Verify test success by checking the pytest output shows "passed" with no "FAILED" or "ERROR"
            - **MANDATORY DOCUMENTATION**: Update documentation for ALL user-facing changes:
              * If you modify src/nyxgpt/api.py or src/nyxgpt/app.py → Update docs/api.md
              * If you modify src/nyxgpt/cli.py → Update docs/configuration.md or README.md CLI section
              * If you add/change config options → Update example.config.ini AND docs/configuration.md
              * If you add new features → Update README.md feature list
              * For ANY code change, check if docs/ files need updates
            - Commit changes with clear messages that MUST include issue references.

            CRITICAL COMMIT MESSAGE FORMAT:
            All commit messages MUST reference issue numbers using this format:

            For parent issues:
            "<type>: <description> (#${{ github.event.issue.number }})"

            For sub-issues (Acceptance Failures):
            "<type>: <description> (#PARENT, #${{ github.event.issue.number }})"

            Examples:
            - Parent: "feat: add user authentication (#${{ github.event.issue.number }})"
            - Parent: "fix: resolve null pointer in validator (#${{ github.event.issue.number }})"
            - Sub-issue: "fix: add missing validation for email field (#2604, #${{ github.event.issue.number }})"
            - Sub-issue: "test: add integration tests per review (#2604, #${{ github.event.issue.number }})"

            Valid commit types: feat, fix, test, docs, refactor, chore

            Output: commit(s) pushed to the current branch with proper issue references.

      - name: Post implementation complete comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: '✅ **Developer Agent**: Implementation complete. Running verification checks...'
            });

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node dependencies
        working-directory: web
        run: npm ci

      - name: Verify Tests Pass
        id: verify_tests
        run: |
          echo "Running pytest to verify all tests pass..."
          if ! pytest tests/unit/ -v; then
            echo "::error::Tests failed! Agent should have fixed this before committing."
            echo "tests_failed=true" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          echo "✅ All tests passed"
          echo "tests_failed=false" >> "$GITHUB_OUTPUT"

      - name: Verify Type Checking (Python)
        id: verify_mypy
        run: |
          if git diff --name-only HEAD~1 | grep -q '\.py$'; then
            echo "Python files changed, running mypy..."
            if ! mypy src/; then
              echo "::error::Mypy type checking failed"
              echo "mypy_failed=true" >> "$GITHUB_OUTPUT"
              exit 1
            else
              echo "✅ Mypy passed"
              echo "mypy_failed=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "No Python files changed, skipping mypy"
            echo "mypy_failed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify Type Checking (TypeScript)
        id: verify_tsc
        run: |
          if git diff --name-only HEAD~1 | grep -q '^web/.*\.tsx\?$'; then
            echo "TypeScript files changed, running type check..."
            cd web
            if ! npm run type-check; then
              echo "::error::TypeScript type checking failed"
              echo "tsc_failed=true" >> "$GITHUB_OUTPUT"
              exit 1
            else
              echo "✅ TypeScript type check passed"
              echo "tsc_failed=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "No TypeScript files changed, skipping type check"
            echo "tsc_failed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify Web Routes
        id: verify_routes
        run: |
          if git diff --name-only HEAD~1 | grep -q '^web/\|^src/nyxgpt/app\.py$'; then
            echo "Web files changed, validating routes..."
            chmod +x scripts/validate-web-routes.sh
            if ! ./scripts/validate-web-routes.sh; then
              echo "::error::Web route validation failed"
              echo "routes_failed=true" >> "$GITHUB_OUTPUT"
              exit 1
            else
              echo "✅ Web routes validated"
              echo "routes_failed=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "No web files changed, skipping route validation"
            echo "routes_failed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Post verification failure comment
        if: failure()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: `❌ **Developer Agent**: Verification failed!

            The implementation did not pass all validation checks. This should not happen - the agent is instructed to validate before committing.

            **What failed:**
            - Check the [workflow logs](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details

            **Next steps:**
            1. Review the workflow logs to see which validation failed (pytest, mypy, tsc, or routes)
            2. The agent will need to be re-assigned to fix the issues
            3. Unassign and re-assign @${{ vars.DEV_AGENT }} to trigger a new implementation attempt

            **Note:** No PR was created because validation failed.`
            });

      - name: Submit PR for review
        if: steps.check_pr.outputs.pr_exists == 'false' && success()
        id: pr
        shell: bash
        run: |
          set -euo pipefail
          ISSUE="${{ github.event.issue.number }}"
          echo "::group::Submitting PR for review"
          PR_NUMBER=$(./scripts/agents/developer_submit_for_review.sh "$ISSUE")
          echo "::notice::Created PR #$PR_NUMBER"
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "::endgroup::"

      - name: Post PR created comment
        if: steps.check_pr.outputs.pr_exists == 'false'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVELOPER_AGENT_TOKEN }}
          script: |
            const issue_number = context.payload.issue.number;
            const pr_number = '${{ steps.pr.outputs.pr_number }}';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: `🎉 **Developer Agent**: Implementation complete!\n\n📝 Pull Request: #${pr_number}\n📊 Status: **In Review**\n👤 Assigned to: @${{ vars.REVIEW_AGENT }}\n\nThe review agent will now:\n1. Wait for CI checks to complete\n2. Review the code changes\n3. Either merge or request changes\n\n[View PR](https://github.com/${{ github.repository }}/pull/${pr_number})`
            });