name: Retro Dashboard - Dump Spend Telemetry

# Derives per-issue spend telemetry (Claude-invoking workflow steps, total
# workflow runs, runner minutes, retry/self-heal cycle count) from GitHub
# Actions run history and publishes scripts/retrospective/data/spend.json to
# the retro data branch. Owner-endorsed direction from the 2026-08-09
# cost post-mortem (#3696): this is the raw signal an anomaly-watching
# process reads to judge whether spend on an issue is unusual -- not a fixed
# cap. Read-only against GitHub Actions; writes only the JSON snapshot. Used
# by the owner's retrospective dashboard.
#
# The snapshot does NOT get pushed at the dispatching branch: the default
# branch's ruleset requires a pull request, and the direct push was rejected
# with GH013 (#3815). publish_data_branch.sh puts it on claude/retro-data and
# retro_data_merge.yml lands it through an auto-merged PR.

on:
  workflow_dispatch:

jobs:
  dump:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Dump spend telemetry
        env:
          GH_TOKEN: ${{ secrets.SCRUMMASTER_AGENT_TOKEN }}
          REPO: ${{ github.repository }}
        run: python3 scripts/retrospective/dump_spend.py

      - name: Publish snapshot to the retro data branch
        env:
          BASE_REF: ${{ github.event.repository.default_branch }}
        run: |
          scripts/retrospective/publish_data_branch.sh \
            "chore(retro): refresh spend telemetry snapshot" \
            scripts/retrospective/data/spend.json
